// src/js/app.js
// Import the CSS entry point ONLY if not handled by vite.config.js input
// (In our config, CSS is a separate input, so Vite handles it)
import '../css/main.css';

console.log('Bailanysta App Initialized!');

// Add any JS logic here later (e.g., Alpine.js, Stimulus, custom scripts)